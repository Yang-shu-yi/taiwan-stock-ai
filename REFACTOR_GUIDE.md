# Taiwan Stock AI 重構指南

## 目的

這份文件用來定義 `taiwan-stock-ai` 下一階段的重構方向。

目標不是繼續修補目前這個混合了 Streamlit、Pi、API 的應用，而是把它重構成一個更清楚的「台股優先情報系統」，具備以下特性：

- 以台股為主
- 美股作為輔助市場脈絡
- 不再涵蓋加密貨幣
- 測試階段以 Telegram 為主要推送管道
- 通知格式適合手機閱讀
- 以自動挑選候選股取代手動管理觀察清單


## 目前存在的問題

### 1. 產品焦點過於分散

目前專案同時混合了：

- Streamlit dashboard
- 每日 AI 報告
- 盤中警示 bot
- Android client
- 手動 watchlist 管理
- 台股 / 美股 / crypto 混合掃描

這會讓產品方向變得模糊。以實際日常使用來看，真正有價值的核心其實是通知流程，而不是 dashboard。

### 2. Streamlit 已經不是合適的中心

原因如下：

- 沒有穩定的每日自動更新流程
- 互動模型對手機使用情境來說太重
- 手動瀏覽資訊的價值低於主動推送
- Android dashboard 本質上只是把 Streamlit 包進 WebView

結論：Streamlit 應該從主要使用流程中退場。

### 3. 手動管理 watchlist 的價值偏低

手動新增 / 刪除股票會佔用畫面與注意力，但無法真正解決核心問題：自動找出最值得關注的股票。

結論：應以「自動挑選候選股」取代手動 watchlist 作為主要機制。

### 4. 通知訊息對手機閱讀不夠友善

目前訊息通常過長、層次不清，也沒有針對 Telegram / 手機閱讀做優化。

結論：通知格式要重設，改成短、分層、可快速決策的高訊號訊息。

### 5. 掃描宇宙過於鬆散

Crypto 應該移除。美股應該只保留少數具有代表性的標的。台股則應該夠廣，能找到機會，但也要經過篩選，避免噪音過高。


## 目標產品方向

重構後的產品應該以這樣的方式運作：

1. 每個交易日掃描一個明確定義的台股優先股票池。
2. 整合市場、資金流、新聞與技術面訊號。
3. 自動排序出台股中最值得關注的候選股票。
4. 加上一小段美股市場脈絡，避免台股判斷與全球市場脫節。
5. 發送簡潔的 Telegram 訊息：
   - 開盤前摘要
   - 盤中警示
   - 收盤後摘要
   - 當日最具潛力股票
6. 測試階段暫停 LINE 推送。


## 核心重構決策

### 1. 完全移除 crypto

需要變更的範圍：

- 移除預設 crypto seed symbols
- 從主要掃描流程中移除 crypto session 邏輯
- 從通知內容中移除 crypto
- 讓產品主體明確回到股票市場

如果未來真的要重新加入 crypto，也應該是獨立模式，而不是混在同一套排序與通知流程裡。

### 2. 台股優先，美股次要

排序引擎與機會偵測應以台股為核心。

建議的輸出注意力分配：

- 台股：80% 到 90%
- 美股：10% 到 20%，作為市場脈絡與少量重點標的

美股的主要用途是：

- 判斷全球市場 risk-on / risk-off
- 理解半導體與 mega-cap 方向
- 輔助解讀台股族群脈絡

美股不應該主導整體通知流。

### 3. Telegram 優先，LINE 先停用

測試階段規則：

- 所有測試訊息只發 Telegram
- 不發 LINE 測試通知

建議設定方向：

- `ENABLE_TELEGRAM=true`
- `ENABLE_LINE=false`
- 可選擇加上 `NOTIFY_CHANNELS=telegram`

等 Telegram 訊息格式與警示品質穩定後，再考慮恢復 LINE。

### 4. Streamlit 退出主要工作流

決策如下：

- 不再投入額外設計精力在 Streamlit 上
- 不再把 dashboard 視為主要使用方式
- 產品中心改為「排程 pipeline + Telegram 推送」

現有 UI 的處理方向：

- 將 `app.py` 標示為 deprecated
- 後續移除 Android dashboard tab，因為它依賴 Streamlit
- FastAPI 只在還需要輕量管理介面或 admin API 的情況下保留

### 5. 用自動候選股取代手動 watchlist

系統應該每天自動產出候選股票池，依據來源包括：

- 價格 / 成交量行為
- 趨勢品質
- 資金流或法人訊號
- 新聞相關性
- 族群脈絡

手動 watchlist 只保留成內部 override 機制，而不再是主要使用者功能。

建議規則：

- 移除使用者面向的手動新增 / 刪除 UI
- 保留一個可選的內部檔案式 override list


## 建議掃描宇宙

## 台股宇宙

台股建議採用雙層股票池。

### Layer A：核心策略股票池

維持一份固定的台股核心清單，規模約 60 到 80 檔，涵蓋：

- 半導體晶圓代工
- IC 設計
- AI server / ODM / OEM
- PCB / ABF / CCL
- 記憶體
- 電源 / 散熱 / 連接器
- 網通 / 光通訊 / 工業電腦
- 必要時納入少量金融或航運作為市場 regime 判讀

這一層的目的是：即使短期流動性條件不佳，市場關鍵族群龍頭也不會被排除在掃描之外。

### Layer B：動態流動性股票池

額外加入約 120 到 170 檔台股，並每週或每月依條件更新，篩選條件例如：

- 僅限普通股
- 排除 ETF / ETN / 特別股 / 權證
- 20 日平均成交值高於門檻
- 20 日平均成交量高於門檻
- 股價高於最低門檻
- 排除極度不活躍個股

建議的初始台股掃描總數：

- 總計 180 到 250 檔

這個規模夠大，能找出機會，也仍然能讓排序與通知品質維持可控。

### 台股輸出規模

不要把所有掃描結果都推送出去。真正輸出的名單應大幅縮小：

- 每日 top picks：6 到 10 檔
- 當日關注清單：12 到 20 檔
- 盤中警示對象：只有高排名且出現強確認訊號的股票

### 台股篩選建議

建議初版可從以下條件開始：

- 僅限上市 / OTC 普通股
- 20 日平均成交值 >= 新台幣 8,000 萬到 1.5 億
- 20 日平均成交量 >= 800 到 1,500 張
- 股價 >= 新台幣 15 元
- 核心戰略龍頭可設例外，即使某個條件未達標也納入

這是較適合台股的平衡點。若全市場都掃，噪音會大幅增加，反而會削弱排序模型的品質。


## 美股宇宙

美股覆蓋應刻意維持小而精。

建議規模：

- 12 到 20 檔

建議組成：

- 市場 regime ETF：`SPY`, `QQQ`, `DIA`, `IWM`, `SOXX`
- 半導體核心：`NVDA`, `AVGO`, `AMD`, `MU`, `TSM`
- mega-cap 脈絡：`MSFT`, `AAPL`, `AMZN`, `META`, `GOOGL`
- 可選高關注個股：`TSLA`

美股的用途不是做廣泛選股，而是：

- 市場脈絡
- 交叉確認
- 族群外溢效應判讀

如果美股訊息太頻繁，就代表它已經偏離產品目標。


## 候選股挑選模型

應以排序引擎取代目前以 watchlist 為中心的模型。

### 候選股評分維度

建議第一版如下：

- 35% 技術面趨勢品質
- 20% 動能與量能確認
- 20% 新聞相關性與情緒
- 15% 法人 / 資金流支持
- 10% 市場 regime 適配度

### 技術面趨勢品質

可使用的訊號包括：

- 股價站上 MA20 / MA60
- 均線排列
- 近期突破或拉回後結構
- RSI 不過度極端
- 相對大盤強弱

### 動能與量能

可使用的訊號包括：

- 日線或盤中的價格擴張
- 相對平均量的量能放大
- 突破近期區間或壓力

### 新聞相關性

新聞不應只拿來產生文字摘要，而應該直接成為排序輸入的一部分。

高價值新聞類型包括：

- 財報 / 展望
- 資本支出或訂單能見度
- AI 供應鏈需求
- 政策支持
- 產業價格或庫存變化
- 重大法人觀點或公司事件

### 市場 regime 適配度

例如：

- 若美股半導體轉弱，台股高 beta 半導體要有更強確認才納入
- 若台股大盤廣度不佳，就降低激進多方候選股權重
- 若 VIX 或總體風險升高，就降低警示頻率


## 通知格式重設

通知必須針對「使用者在手機上幾秒內閱讀」這件事去設計。

### 設計規則

- 一則訊息只傳達一個明確目的
- 每行盡量短
- 避免長段落
- 先寫結論，再寫理由
- 欄位順序固定
- 摘要訊息與個股警示分開

### 訊息類型

#### 1. 開盤前摘要

目的：

- 市場開盤前的整體設定
- 台股主題方向
- 美股隔夜脈絡
- 今日重點股

建議格式：

```text
[開盤前]
市場：中性偏多
美股：SOXX +1.4%，NVDA 偏強
台股焦點：AI server、PCB、散熱
重點股：3-5 檔
操作：觀察開盤強弱，避免追弱開
```

#### 2. 盤中警示

目的：

- 傳達單一可執行事件

建議格式：

```text
[盤中警示]
股票：2382
狀態：突破確認
價格：123.5（+3.8%）
原因：量增 + 族群轉強 + 新聞支持
風險：125 以上不宜追價
```

#### 3. 收盤後摘要

目的：

- 今天發生了什麼
- 哪些族群有效
- 哪些股票值得隔日延續追蹤

建議格式：

```text
[收盤後]
台股：+0.9%，市場廣度改善
領漲：AI server、光通訊
弱勢：航運、防禦型輪動
延續觀察：3 檔隔日名單
```

#### 4. 每日潛力股清單

目的：

- 給出高排名股票，但不讓使用者資訊過載

建議格式：

```text
[今日潛力股]
1. 2382 - 趨勢 + 量能 + 新聞
2. 3231 - 拉回後修復結構
3. 3661 - 族群領先
```

### Telegram 實作建議

- 可以使用 Telegram 基本格式化，但不要過度裝飾
- 每則訊息控制在 5 到 8 行左右
- 如果內容較長，依用途拆成多則訊息
- 不要把完整 AI 長文直接丟給使用者
- 長版分析可保留在內部紀錄或 review 用輸出


## 重構後的產品流程

### 每日流程

1. 建立台股與美股掃描宇宙。
2. 取得市場與新聞資料。
3. 為所有候選股打分。
4. 選出台股 top candidates，加上一小段美股脈絡。
5. 產出簡潔的 Telegram 摘要。
6. 存下結構化快照，供後續檢查與回顧。

### 盤中流程

1. 只監控當日高排名或當日活躍的台股候選。
2. 只有在價格、量能、脈絡都確認時才觸發警示。
3. 使用 cooldown 避免重複洗版。
4. 僅發 Telegram。


## 建議的架構重整

目前程式碼以 script 為中心，這對原型開發很快，但現在已成為維護成本與功能漂移的主要來源。

建議的目標目錄結構如下：

```text
config/
  settings.py

data_sources/
  yahoo_client.py
  twse_client.py
  finmind_client.py
  news_client.py

universe/
  tw_universe.py
  us_universe.py

scoring/
  technical_score.py
  news_score.py
  flow_score.py
  ranker.py

selection/
  candidate_selector.py

notifications/
  telegram_sender.py
  message_formatter.py

pipelines/
  pre_market.py
  post_market.py
  intraday.py

storage/
  snapshot_store.py
  alert_store.py

tools/
  dry_run.py
```

### 舊檔案到新架構的對應

- `rpi_main.py` -> 拆成 `pre_market.py` 與 `post_market.py`
- `rpi_intraday.py` -> 拆成 `intraday.py` + `message_formatter.py`
- `market_data.py` -> 拆成各資料來源 client
- `news_scraper.py` -> `news_client.py`
- `report_generator.py` -> 拆成較小的 summarizer 與 formatter 模組
- `app.py` -> deprecated
- `batch_scan.py` -> 改成真正的 headless scan pipeline，或直接移除
- `api_server.py` -> 只有仍需要 mobile/admin API 時才保留


## 建議的遷移計畫

### Phase 1：產品簡化

- 從所有預設流程移除 crypto
- 所有測試通知改為 Telegram-only
- 將 Streamlit 凍結為 deprecated
- 停止擴充手動 watchlist 功能

### Phase 2：通知改寫

- 建立新的 Telegram 訊息 formatter
- 重設開盤前、盤中、收盤後訊息模板
- 增加 dry-run 模式，先預覽訊息再正式發送

### Phase 3：股票池與排序

- 建立固定的台股核心股票池
- 建立動態台股流動性股票池
- 建立小型美股脈絡股票池
- 建立候選股排序 pipeline
- 自動輸出 top picks

### Phase 4：整理 pipeline

- 將巨大的單檔 script 拆成模組
- 集中設定管理
- 減少大量吞例外的寫法
- 補上結構化 logging

### Phase 5：整理 UI / API

- 移除或隱藏 Streamlit 入口
- 移除 Android 對 Streamlit dashboard 的依賴
- 保留 FastAPI 作為 admin-only API，或視情況退場


## 測試策略

在重新啟用 LINE 之前，所有測試都應以 Telegram 為唯一通知管道。

### 建議的測試層次

- 排序邏輯的 unit tests
- 通知格式的 unit tests
- 訊息輸出的 dry-run snapshot tests
- 使用快取樣本資料的 provider integration tests
- 私人 Telegram chat 的端對端測試

### 操作安全規則

- 應提供 `DRY_RUN=true`
- 重構期間預設 `ENABLE_LINE=false`
- 長篇 AI 報告應寫入 log / file，而不是直接推送給使用者


## 額外值得做的改善

### 1. 分離機器可讀資料與人類可讀文字

評分結果先以 JSON 等結構化格式存下來，最後一步才格式化成訊息。

### 2. 增加快照歷史

保存每日排序結果與警示決策，讓後續可以回頭檢查為什麼某檔股票被選中。

### 3. 降低 silent failure

目前很多流程會吞例外後直接回空值。應改為：

- warning logs
- degraded mode 標記
- 資料來源健康度摘要

### 4. 提示詞版本化

如果 AI prompt 不做版本管理，輸出品質會漂移，而且很難追蹤。

### 5. 定義成功指標

可追蹤：

- 每日警示數量
- 若可行則追蹤點開率 / 閱讀率
- 重複或低品質警示比例
- top picks 的隔日延續表現


## 建議的第一階段實作範圍

第一輪真正進入重構時，範圍要刻意收斂：

1. Telegram-only 推送
2. 移除 crypto
3. 將 Streamlit 標示為 deprecated
4. 移除使用者面向的手動 watchlist 流程
5. 建立台股優先的掃描宇宙
6. 重寫通知模板
7. 輸出台股 top opportunities + 小型美股脈絡

不要一開始就試圖重做所有東西。


## Refactor V1 的驗收標準

- 掃描與通知中不再出現 crypto
- Telegram 是唯一啟用的測試通知管道
- 開盤前與收盤後訊息在手機上 10 秒內可讀完
- 盤中警示短且可執行
- 台股掃描宇宙明確、可控
- 美股宇宙維持在 20 檔以下
- 手動 watchlist 不再是主要使用流程
- 日常使用不再依賴 Streamlit


## 我的建議

最合理的方向是：

- 把它做成 Telegram-first 的股票情報系統
- 重點放在台股機會排序
- 美股只作為市場脈絡
- 停止再投資 dashboard-first 的工作流

如果照這個方向走，程式會更小、可維護性更高，也更符合你真正想使用它的方式。
